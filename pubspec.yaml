name: smartglasses_seniorproject
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.68.0
  http: ^1.1.0
  pdf: ^3.12.0
  file_saver: ^0.3.1
  media_store_plus: ^0.1.3
  path_provider: ^2.1.5
  flutter_slidable: ^3.0.0
  cupertino_icons: ^1.0.8
  firebase_core: ^3.0.0
  firebase_auth: ^5.7.0
  firebase_messaging: ^15.0.0
  provider: ^6.1.2
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
